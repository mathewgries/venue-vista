import React from 'react'

export default function PostsFeed(props){
    return (
        <div className='posts-feed-container'>
            <p>Posts Feed</p>
            {/* This could be a side by side view as well */}

            {/* Left side contains the logic to add a post */}
            
            {/* Right side is a scrolling feed of posts by others and user */}
        </div>
    )
}