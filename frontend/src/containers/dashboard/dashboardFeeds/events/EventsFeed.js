import React from 'react'

export default function EventsFeed(props) {
    return (
        <div className='events-feed-container'>
            <p>Events Feed</p>
            {/* I would like to have a side by side view */}

            {/* Left view - current month calendar - Should 
                be able to toggle month and year  possibly click 
                day to see all events on that day*/}

            {/* Right view - scrolling list of events in order of event date */}
        </div>
    )
}